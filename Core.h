#pragma once



#include <iostream>      // for input/output
#include <vector>        // for std::vector
#include <string>        // for std::string
#include <algorithm>     // for sort, max, min, etc.
#include <cmath>         // for math functions like sqrt, pow, etc.
#include <cstdlib>       // for general purpose functions like rand, malloc, etc.
#include <ctime>         // for time functions
#include <stack>         // for std::stack
#include <queue>         // for std::queue, std::priority_queue
#include <deque>         // for std::deque
#include <set>           // for std::set, std::multiset
#include <map>           // for std::map, std::unordered_map
#include <unordered_map> // for std::unordered_map
#include <unordered_set> // for std::unordered_set
#include <bitset>        // for binary representation
#include <climits>       // for INT_MAX, INT_MIN
#include <limits>        // for numeric_limits
#include <cassert>       // for assert()
#include <fstream>       // for file input/output
#include <sstream>       // for stringstream
#include <iomanip>       // for setprecision, setw, etc.
#include <functional>    // for function objects like greater<int>
#include <iterator>      // for iterators


using namespace std;